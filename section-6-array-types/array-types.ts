const activeUsers: Array<string> = [];

activeUsers.push("Diego");

const ages: number[] = [45, 34, 12, 1];
ages[0] = 21;

type Point = {
	x: number;
	y: number;
};

const coords: Array<Point> = [];

coords.push({ x: 23, y: 0 });
